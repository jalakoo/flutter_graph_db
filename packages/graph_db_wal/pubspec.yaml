name: graph_db_wal
description: >-
  WAL schema, CBOR encoding, recovery protocol, and the byte-range
  `WalStore` port (plan §6). Stub — implementation lands in Phase 2.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.5.0

dependencies:
  graph_db_core:
    path: ../graph_db_core
  # Pinned per the §13 dependency audit. Wired in Phase 2 once the
  # hot-shape hand-rolled encoders land alongside.
  cbor: ^6.5.1
  xxh3: ^1.2.0
  uuid: ^4.5.0

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
