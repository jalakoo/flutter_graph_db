# graph_db_wal

WAL schema, CBOR encoding, recovery, and the byte-range `WalStore` port
— plan §6.

**Status:** Phase 0 skeleton. Implementation lands in Phase 2 per plan
§14. See the top-of-file library doc for the scope.
