# graph_db_remote

`RemoteGraphClient` interface + error hierarchy + capability flags —
plan §9.

**Status:** Phase 0 skeleton. Implementation lands in Phase 4 per plan
§14. Backend adapters live in `graph_db_remote_neo4j` and
`graph_db_remote_falkor`.
