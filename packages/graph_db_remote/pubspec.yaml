name: graph_db_remote
description: >-
  `RemoteGraphClient` interface, error hierarchy, and capability flags
  (plan §9). No backend implementations live here — adapters depend on
  this package. Stub — implementation lands in Phase 4.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.5.0

dependencies:
  graph_db_core:
    path: ../graph_db_core

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
