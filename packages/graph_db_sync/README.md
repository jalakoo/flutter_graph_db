# graph_db_sync

Push (and eventually bidirectional) sync engine — plan §10.

**Status:** Phase 0 skeleton. Implementation lands in Phase 7 per plan
§14. Depends on `graph_db_core`, `graph_db_wal`, `graph_db_remote`.
