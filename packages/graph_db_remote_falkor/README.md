# graph_db_remote_falkor

RESP-over-Dart `RemoteGraphClient` adapter for FalkorDB — plan §9.1.

**Status:** Phase 0 skeleton. Implementation lands in Phase 4 per plan
§14. `package:redis` is the candidate per §13; minimal RESP port is the
fallback (~1 week).
